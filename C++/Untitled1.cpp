#include<iostream>
using namespace std;
#include<cmath>

int main(){
    float a;
    cout << "Nhap a: ";
    cin >> a;
    cout >> "Can bac 2 cua a la: " << sqrt(a);
    cout << "\na^5 la: " << pow(a, 5);
    return 0;
}
